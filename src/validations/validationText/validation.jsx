import React from "react";
import "./style.css";

const Validation = (props) => {
    return(
        <div className={"validation"}>
            { props.children }
        </div>
    )
}
export default Validation